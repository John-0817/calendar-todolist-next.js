import { outfit } from '@/app/ui/font'

export default function Calendar() {
  return (
    <main>
      <h1 className={`${outfit.className} text-5xl font-semibold`}>Calendar</h1>
      
    </main>
  )
}